package com.visionmatrix.ctrlf.actioncards.model

/**
 * Minimal placeholder model for a scene action card.
 * Extend with real fields (title, description, actions, media, etc.).
 */
data class ActionCard(
    val id: String = "",
    val title: String = ""
)
